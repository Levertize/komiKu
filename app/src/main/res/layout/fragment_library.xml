<?xml version="1.0" encoding="utf-8"?>
<!--
  LIBRARY FRAGMENT
  Komponen: Header + local search + RecyclerView grid 2 kolom
  Adapter: LibraryAdapter (GridLayoutManager 2 kolom)
  Item layout: item_library_card.xml
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/dk_bg">

    <!-- Header -->
    <LinearLayout
        android:id="@+id/lib_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:paddingTop="12dp"
        android:paddingBottom="10dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/tv_library_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/library_title"
            android:textSize="@dimen/text_xl"
            android:textStyle="bold"
            android:textColor="@color/dk_text"
            android:fontFamily="@font/plus_jakarta_sans_bold"
            android:letterSpacing="-0.02" />

        <TextView
            android:id="@+id/tv_library_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0 komik tersimpan"
            android:textSize="10sp"
            android:textColor="@color/dk_muted"
            android:layout_marginTop="1dp" />

        <!-- Local search -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="36dp"
            android:layout_marginTop="10dp"
            android:background="@drawable/bg_search_box">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:orientation="horizontal"
                android:paddingStart="12dp"
                >

                <ImageView
                    android:layout_width="12dp"
                    android:layout_height="12dp"
                    android:src="@drawable/ic_search"
                    app:tint="@color/dk_hint"
                    android:alpha="0.5" />

                <EditText
                    android:id="@+id/et_library_search"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:hint="@string/hint_search_library"
                    android:textSize="10sp"
                    android:textColor="@color/dk_text"
                    android:textColorHint="@color/dk_hint"
                    android:background="@null"
                    android:inputType="text"
                    android:maxLines="1" />

            </LinearLayout>

        </FrameLayout>

    </LinearLayout>

    <!-- Bottom border -->
    <View
        android:id="@+id/header_divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/dk_border"
        app:layout_constraintTop_toBottomOf="@id/lib_header"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Empty state (GONE by default) -->
    <LinearLayout
        android:id="@+id/layout_empty"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:gravity="center"

        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/header_divider"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:src="@drawable/ic_empty_library"
            app:tint="@color/dk_hint" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Koleksi masih kosong"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="@color/dk_muted" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tambahkan komik favoritmu\nke library dengan ikon bookmark"
            android:textSize="11sp"
            android:textColor="@color/dk_hint"
            android:gravity="center"
            android:lineSpacingExtra="2dp" />

    </LinearLayout>

    <!-- Library grid RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_library"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:padding="12dp"
        android:overScrollMode="never"
        android:scrollbars="none"
        app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
        app:spanCount="2"
        app:layout_constraintTop_toBottomOf="@id/header_divider"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
